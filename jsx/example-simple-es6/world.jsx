/** @jsx React.DOM */

let worldES6Init = (app, window, $, React, undefined) => {
  'use strict';

  return React.createClass({
    render() {
      return (
        <span>
          <strong>world</strong>
        </span>
      );
    }
  });
};

export default worldES6Init;
